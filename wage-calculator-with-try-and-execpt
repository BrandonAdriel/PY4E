try:
    hrs = float(input('Enter hours: '))
    rate = float(input('Enter rate: '))
    reg = hrs * rate

    if hrs > 40:
        ot = (hrs-40) * (rate*0.5) 
        pay = reg + ot
    else:
        pay = reg
    print(f"Pay: {pay}")

except ValueError:
    print("Please Enter Number")
